server:
  port: 8080
spring:
  application:
    name: Demo API
  profiles:
    active: dev
    include:
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: false
  jackson:
    serialization.indent_output: true